apiVersion: v1
kind: Service
metadata:
  name: products
  labels:
    app.kubernetes.io/name: products
    app.kubernetes.io/part-of: microservices
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: products
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: products
  labels:
    app.kubernetes.io/name: products
automountServiceAccountToken: false
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: products
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: products
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: products
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: products
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: products
  labels:
    app.kubernetes.io/name: products
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: products
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: products
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: products
  policyTypes: [Ingress]
  ingress:
    # traffic from other services in the namespace
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/part-of: microservices
      ports:
        - port: 8080
    # Prometheus scraping
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - port: 8080
